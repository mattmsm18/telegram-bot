services:
  - type: worker
    name: telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    runtime: python
    buildEnv:
      PYTHON_VERSION: 3.11.9
